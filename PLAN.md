# 贪吃蛇实现计划

用户已确认方案（2026-09-17）。

## 目标

编写可直接运行的 `code/snake.py`，使用 Python 标准库 tkinter 显示窗口，
提供方向键操作、食物、计分、碰撞结束、暂停和重新开始。
游戏规则与窗口分离，便于使用 unittest 自动验证。

## 进度

- [x] 确认方案与运行环境：Python 3.14.5、Tk 8.6 可用。
- [x] 检查 Git：工作区初始干净，起始版本 `93cc59e`。
- [ ] 实现游戏逻辑和窗口。
- [ ] 编写并通过自动化测试，验证移动、转向、增长、碰撞、暂停和胜利。
- [ ] 检查窗口操作，补充运行命令与手动测试步骤。
- [ ] 更新 CHANGELOG.md 并提交 Git，记录实现版本。

## 验收方法

1. `python code/snake.py` 能打开窗口并正常游玩。
2. `python -m unittest discover -s tests -v` 验证主要规则和边界情况。
3. 手动检查方向键、空格暂停、R/回车重开、碰撞结束与关闭窗口。

## 范围

无需第三方包；保留现有 Hello World 示例与实验材料。
不启动 Node.js 进程。
